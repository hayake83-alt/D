@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;600;700&family=Inter:wght@300;400;500;600&display=swap');

:root{
  --bg:#0d1017;
  --panel:rgba(21,26,36,.82);
  --text:#f4f1ea;
  --muted:#aaa7a0;
  --accent:#8f1f2c;
  --accent-2:#c49a6c;
  --line:rgba(255,255,255,.12);
}

*{box-sizing:border-box}
html,body{margin:0;min-height:100%;background:
radial-gradient(circle at 20% 20%, rgba(143,31,44,.16), transparent 30%),
radial-gradient(circle at 80% 75%, rgba(196,154,108,.11), transparent 30%),
var(--bg);color:var(--text);font-family:Inter,system-ui,sans-serif;overflow-x:hidden}

body{min-height:100vh}
.noise{position:fixed;inset:0;pointer-events:none;opacity:.06;
background-image:url("data:image/svg+xml,%3Csvg viewBox='0 0 180 180' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.9' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='.5'/%3E%3C/svg%3E")}

#app{min-height:100vh;display:grid;place-items:center;padding:28px 18px 70px}
.scene{display:none;width:100%;max-width:760px;animation:fade .7s ease}
.scene.active{display:block}
@keyframes fade{from{opacity:0;transform:translateY(12px)}to{opacity:1;transform:none}}

.card{background:var(--panel);backdrop-filter:blur(14px);border:1px solid var(--line);
border-radius:24px;padding:34px 24px;box-shadow:0 20px 60px rgba(0,0,0,.35)}
.card.wide{max-width:760px}
.eyebrow{display:block;font-size:.72rem;letter-spacing:.18em;color:var(--accent-2);margin-bottom:18px}
h1,h2,h3{font-family:"Cormorant Garamond",serif;margin:0}
h1{font-size:clamp(4.3rem,18vw,8rem);line-height:.9}
h2{font-size:clamp(2.5rem,10vw,4.6rem);line-height:1;margin-bottom:18px}
h3{font-size:2.5rem;color:var(--accent-2);margin:.2rem 0 1rem}
p{line-height:1.72;color:#e8e4dc}
.lead{font-size:1.25rem}
.big-pause{font-family:"Cormorant Garamond",serif;font-size:2.3rem;margin:30px 0 5px}

button{border:0;cursor:pointer;font:inherit}
.primary,.secondary,.capsule{margin-top:24px;border-radius:999px;padding:14px 20px;transition:.25s ease}
.primary{background:var(--text);color:#111318;font-weight:600}
.primary:hover{transform:translateY(-2px)}
.secondary{background:transparent;color:var(--text);border:1px solid rgba(255,255,255,.28)}
.capsule{background:linear-gradient(135deg,var(--accent),#471018);color:white;width:100%;font-weight:600;
box-shadow:0 10px 30px rgba(143,31,44,.25)}

.timeline{margin:28px 0;position:relative;padding-left:30px}
.timeline-line{position:absolute;left:7px;top:0;bottom:0;width:2px;background:var(--line)}
.timeline-line span{display:block;width:100%;height:0;background:var(--accent-2);transition:height 2.4s ease}
.memory{margin:0 0 24px;padding:0 0 0 10px;color:#ddd8cf;opacity:.4;transform:translateX(-8px);transition:.6s}
.memory.visible{opacity:1;transform:none}

.diploma{border:1px solid rgba(196,154,108,.38);padding:26px 20px;text-align:center;background:rgba(255,255,255,.025);position:relative}
.seal{width:56px;height:56px;border:1px solid var(--accent-2);border-radius:50%;display:grid;place-items:center;
margin:0 auto 18px;font-family:"Cormorant Garamond",serif;font-size:2rem;color:var(--accent-2)}
.small{font-size:.83rem;color:var(--muted)}

.hidden-message{max-height:0;overflow:hidden;opacity:0;transition:max-height 1s ease,opacity .8s ease,margin .8s ease}
.hidden-message.open{max-height:600px;opacity:1;margin-top:22px}
.hidden{display:none}

.future-grid{display:grid;grid-template-columns:1fr;gap:12px;margin-top:28px}
.future-grid span{border:1px solid var(--line);border-radius:16px;padding:14px;color:#d9d5cd;
background:rgba(255,255,255,.02)}

.diagnosis{border-left:2px solid var(--accent);padding-left:18px}
.final-card{text-align:center;position:relative;overflow:hidden}
.floating-psi{font-family:"Cormorant Garamond",serif;font-size:8rem;color:rgba(196,154,108,.08);
position:absolute;right:-12px;top:-28px;transform:rotate(8deg)}
.congrats{font-family:"Cormorant Garamond",serif;font-size:2rem;margin-bottom:0}
.done{font-size:1.05rem;letter-spacing:.18em;text-transform:uppercase;color:var(--accent-2)}
.last strong{font-family:"Cormorant Garamond",serif;font-size:2rem;color:var(--text)}

.progress-dots{position:fixed;left:50%;bottom:24px;transform:translateX(-50%);
display:flex;gap:7px;padding:9px 12px;border-radius:999px;background:rgba(8,10,15,.55);backdrop-filter:blur(10px)}
.dot{width:6px;height:6px;border-radius:50%;background:rgba(255,255,255,.23);transition:.3s}
.dot.active{width:20px;border-radius:999px;background:var(--accent-2)}

@media(min-width:700px){
  .card{padding:52px}
  .future-grid{grid-template-columns:1fr 1fr}
}
